In questa directory sono presenti i principali file.jar che permettono di giocare a Backgammon, sfidando l'algoritmo ExpectedMinMax nelle differenti versioni (funzioni di valutazione).

Comando:

java -jar file.jar

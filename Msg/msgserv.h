/** \file msgserv.h \author Filippo Fontanelli mat 422385 Si dichiara che il contenuto di questo file e' in ogni sua parte opera originale dell' autore.  */#ifndef MSGSERV_H_#define MSGSERV_H_#include "server.h"#include "comsock.h"#include "genHash.h"#include "queue.h"#include "utilityserver.h"#include <signal.h>#include <pthread.h>#include <ctype.h>#define USERDISCONNECT -1/** Termina i worker attivi(non dealloca la memoria) *	\param	client	il nome del client mittente * */void termina_workers(pthread_mutex_t *mtx_worker);/** Writer, legge i messaggi nella coda dei messaggi di log e provvede a scriverlli sul file di log */void* writer();/** Workers, soddisfare le richieste del client, e nei casi necesari inserisce i messaggi nella coda dei messaggi di log. * (setta errno in caso di errore) *	\param	socket	socket sulla quale inviare i messaggi * */void *workers (void *socket);/** Handler, funzione che si occupa della gestione dei segnali. * */void *handler();#endif /* MSGSERV_H_ */